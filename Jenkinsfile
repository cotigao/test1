pipeline {
    agent any
    stages {
        stage ('Checkout') {
            steps {
                sh """
                #!/bin/bash"""
            }
        }
        stage('Build') {
            steps {
                sh 'echo "Hello world!"'
            }
        }
    }
}
